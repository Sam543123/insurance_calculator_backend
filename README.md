# Insurance calculator backend

Insurance calculator backend provides API to get insurance sum, insurance premium, reserve and tariffs table. The backend was written in python using Django Rest Framework web framework.

## Run locally in Docker
Run docker/docker_run.cmd on windows or docker/docker_run.sh on unix. The calculator backend will be available on http://localhost:8000/.
